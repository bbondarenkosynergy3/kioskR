Отлично 🔥
Вот обновлённое и максимально точное ТЗ для APK-оболочки “360 Synergy Kiosk”, адаптированное под твои новые требования — минимум логики, максимум стабильности.

⸻

📱 360 Synergy Kiosk — APK Functional Specification (v2)

🎯 Цель

Создать простое Android-приложение (APK), которое:
	•	открывает страницу https://app.360synergy.net в WebView (fullscreen);
	•	автоматически уходит в “sleep mode” каждые 2 минуты, затем просыпается;
	•	полностью блокирует выход из приложения, кроме скрытого “жеста админа” (тап по четырём углам по очереди).

⸻

🧩 Функциональные требования

1️⃣ WebView оболочка
	•	Приложение загружает https://app.360synergy.net в WebView.
	•	Полноэкранный режим — без статус-бара, навигации и системных элементов.
	•	Включены JavaScript и DOM Storage.
	•	Используется WebViewClient, чтобы все переходы оставались внутри WebView.
	•	Автоматическая перезагрузка страницы при потере соединения.

⸻

2️⃣ Sleep / Wake цикл
	•	Каждые 2 минуты приложение программно уходит в “спящий режим”:
	•	экран выключается (wakeLock.release());
	•	WebView останавливается (например, onPause() вызывается).
	•	Через 2 минуты приложение автоматически “просыпается”:
	•	экран включается (wakeLock.acquire());
	•	WebView обновляется (reload() или onResume()).
	•	Цикл повторяется бесконечно, пока устройство включено.
	•	При ручном пробуждении (пользователь коснулся экрана) таймер перезапускается.

⸻

3️⃣ Блокировка выхода (Kiosk Lock)
	•	Приложение работает в режиме startLockTask() (Single App Mode).
	•	Все системные кнопки (“Home”, “Recent apps”, “Back”) — заблокированы.
	•	Выход из режима возможен только при секретном действии администратора:
Нажатие по четырём углам экрана в правильной последовательности (например:
верх-лево → верх-право → низ-лево → низ-право).
	•	После правильной последовательности появляется диалог:
“Exit kiosk mode?” → Yes/No.
	•	При подтверждении вызывается stopLockTask() и закрывается приложение.

⸻

4️⃣ Автозапуск при включении устройства
	•	При получении BOOT_COMPLETED приложение автоматически запускается.
	•	После старта сразу активирует fullscreen, lockTask и WakeLock-цикл.

⸻

5️⃣ Offline / Reconnect
	•	При потере сети отображается экран “Reconnecting…”.
	•	После восстановления автоматически возвращается на https://app.360synergy.net.

⸻

⚙️ Технические детали

Параметр	Значение
Target Android	10+
Min SDK	29
Язык	Kotlin
Web URL	https://app.360synergy.net
Sleep Interval	2 минуты (120 000 мс)
Permissions	INTERNET, WAKE_LOCK, RECEIVE_BOOT_COMPLETED
Main Components	MainActivity.kt, BootReceiver.kt
APK Size	< 50 MB
Lock Gesture	4 угла, последовательный тап
Fullscreen	Да
Offline-экран	Да (простое LinearLayout сообщение)


⸻

✅ Результат

После установки и перезагрузки устройства:
	•	приложение само запускается;
	•	показывает https://app.360synergy.net в полноэкранном режиме;
	•	каждые 2 минуты уходит в sleep и автоматически просыпается;
	•	пользователь не может выйти или свернуть приложение;
	•	админ может выйти, тапнув по четырём углам экрана подряд.

⸻

Хочешь, я теперь соберу готовый проект (ZIP) под это обновлённое ТЗ — с логикой 4-углового выхода и 2-минутным циклом сна/пробуждения, уже совместимый с Codemagic?